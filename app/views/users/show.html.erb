<div class="user-div">
<div class="photo">
<% if @user.photo.present? %>
  <%= image_tag @user.photo, alt: "User Photo", class: "user-photo" %>
<% else %>
  <span>No photo</span>
<% end %>
</div>
<div class="user-info">
<span class="user-name"><%= @user.name %></span>
<span class="post-count">Number of posts: <%= @user.posts.count %></span>
</div>
</div>

<div class="wrap bio">
  <h4 class="title">Bio</h4>
  <p><%= @user.bio %></p>
</div>

<ul>
  <% @user.recent_posts.each_with_index do |post, index| %>
    <li class="post wrap list">
    <div>
      <h4 class="title list-title">Post <%= index+1  %>: <%= link_to post.title, user_post_path(@user, post) %></h4>
      <p class="post-text"><%= post.text %></p>
    </div>
      <div class="counters">
      <p class="small">Comments: <%= post.comments.count %></p>
      <p class="small">Likes: <%= post.likes.count %></p>
      </div>
    </li>
  <% end %>
</ul>
<div class="see-all-wrap">
<%= link_to user_posts_path(@user) do %>
<span class="see-all">See all posts</span>
<% end %>
</div>
